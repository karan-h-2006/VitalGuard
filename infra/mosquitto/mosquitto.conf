listener 1883
allow_anonymous true

# Phase 0 is local-only. Phase 1 must replace anonymous access with credentials
# before any service reaches a shared environment.
persistence false
log_dest stdout
