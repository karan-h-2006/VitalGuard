/**
 * TODO(phase-1): JWT/session auth middleware hooks in here.
 *
 * Register on the Fastify instance in `app.ts` so feature routes opt *out*
 * of auth (health, public well-known) rather than opt in. Safer default
 * once PHI is flowing through this process.
 */
export {};
