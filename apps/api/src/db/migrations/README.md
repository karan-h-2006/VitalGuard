Migration `0000_new_karma.sql` was generated by `drizzle-kit generate` in Module 2.
It creates all nine tables and six enum types defined in `schema.ts`.
Run `pnpm --filter @vitalguard/api db:migrate` to apply it to your local Docker Postgres.
